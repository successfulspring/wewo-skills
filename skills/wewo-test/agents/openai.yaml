interface:
  display_name: "wewo-test"
  short_description: "Run automated native tests through available agent tools"
  default_prompt: "Use $wewo-test to execute in-scope automatable cases with repository-native routes and runners, select available agent tool interfaces, preserve evidence, exclude Manual cases from execution, and produce an evidence-based automated gate."
